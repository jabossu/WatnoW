<nav class="navbar navbar-expand-sm mx-0 navbar-light bg-light shadow-sm w-100">
  <a class="navbar-brand" href="<%= root_url %>">
    <%= image_tag("icon-256.png", size: "30x30", alt: "WatnoW Icon", class: "d-inline-block align-top") %>
    WatnoW
  </a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <li class="nav-item">
        <%= link_to "#{glyph('home')}Home".html_safe, root_url, class: "nav-link" %>
      </li>
      <li class="nav-item">
        <%= link_to "#{glyph('question')}About".html_safe, about_url, class: "nav-link" %>
      </li>

      <% if logged_in? %>
        <li class="nav-item">
          <%= link_to "#{glyph('scroll')}Cards".html_safe, usercards_url, class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "#{glyph('sign-out-alt')}Log out".html_safe, logout_path, method: :delete, class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "#{glyph('user')} Hi #{@current_user.name} !".html_safe, user_path(current_user), class: "nav-link" %>
        </li>
      <% else %>
        <li class="nav-item">
          <%= link_to "#{glyph('sign-in-alt')}Log in".html_safe, login_url, class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "#{glyph('pen-alt')}Sign up".html_safe, signup_url, class: "nav-link" %>
        </li>
      <%end%> 
    </ul>
  </div>
</nav>